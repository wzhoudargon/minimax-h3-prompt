---
name: minimax-h3-prompt
description: 为 MiniMax H3 编写、改写和检查视频提示词，覆盖文生、首帧、首尾帧、尾帧及多模态素材用途规划，处理对白、旁白、音效和配乐要求。用于 H3 提示词任务；不负责 H3 Studio 程序开发或直接生成视频。
---

# MiniMax H3 Prompt

把创作意图整理为可执行的视听提示词。独立于 H3 Studio、ComfyUI 或任何内容领域；没有生成 API、凭据、付费提交或媒体处理依赖。

## 先匹配实际入口

沿用用户已指定的素材、模式、时长与声音路线。信息足够就编写；缺少影响格式的入口信息时，问一个必要问题，同时可先整理不依赖接口的创意内容。

| 已知输入与目标 | 路由 |
|---|---|
| 无关键帧，文字构建视频 | T2VA |
| 一张图作为真实开头 | I2VA |
| 两张图作为开头和结尾 | FL2VA |
| 一张图只指定结尾 | L2VA |
| 人物、动作、音色等多素材参考或编辑 | 先列素材用途；使用实际入口的规则，不套用关键帧语法 |

- 用户端自然语言：按“参考素材用途＋核心创意＋画面过程”组织。
- 开源结构化路径：读 [formats.md](references/formats.md)，选对应 instruction 和三个字段。
- 涉及对白、旁白、演唱或串读：读 [sound.md](references/sound.md)。
- 判断官方要求、扩展模式或接口限制：读 [sources.md](references/sources.md)，必要时查当前官方资料。

多模态参考中说明每个素材的编号和用途，例如人物形象、场景、动作、音色参考或音频复用。音色参考不等于复用音频；图片作为人物参考也不等于把图片锁成首帧。素材编号服从实际上传列表，不把未上传素材写进提示词。当前没有完整验证全能参考结构化语法，不假装支持自动校验该模式。

## 编写与修订

1. 说明主体、场景、动作、风格和结尾。已有参考图时保留关键身份、服饰、物件及空间锚点，写从当前状态如何继续。
2. 按时间或镜头组织可见、可听事件；交代动作起点、路径、结果和必要的数量／连接约束。保留用户要的多镜或单镜，不默认8秒、竖屏、古风、闭唇或禁字幕。
3. 镜头运动和动作不能自相矛盾。时间预算容纳台词与停顿；结束帧来自连续变化，不把不相容画面当作瞬间变形。
4. 台词、表演指令和声音设计分清用途；原文有保持要求时逐字对照。声音路线以用户意图为准，不自动换成后期配音。
5. 修改既有提示词时保留有效创意与已确认参数，说明实质变化。只有明确要求时才把标题、说明、字幕或台词加入成片。

中文与英文制作指令都可用；语言转换不是已验证的防串读方法。不要把某次诗词或单镜测试的限制推广给广告、多人对白、MV 或其他内容。

## 交付与检查

交付可直接复制的提示词；模式、素材映射、时长参数和待确认事项放在提示词之外。结构化提示词可运行：

```bash
python3 scripts/validate_prompt.py prompt.txt --mode I2VA --duration 8 --image-count 1
```

脚本只做标准结构、字段、关键帧对齐、镜头编号及可选图片数量检查，不评估动作是否合理、对白正确性、媒体格式或输出声音。对不同模式使用真实参数；不固定模型规格或假装当前部署支持所有参数。

单条保留指南排版。只有目标上传器确实要求时才另导出“一行一任务”，保持单条源文件并核对最终送入模型的文本与是否经过自动扩写。该适配不属于 H3 通用规则。

标记完成程度：提示词已编写／结构检查通过／生成结果已审阅，三者分开。串读需实际生成后逐字对照和连续听检；不能凭提示词或脚本通过宣布声音已修复。
